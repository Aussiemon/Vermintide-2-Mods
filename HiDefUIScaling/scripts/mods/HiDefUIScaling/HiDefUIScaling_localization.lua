return {
	mod_description = {
		en = "Provides fine-tuned control over global UI scaling."
	},
	scale_slider = {
		en = "Global UI Scaling Limit"
	},
	scale_description = {
		en = "Choose the global UI scale limit. High values only effect the UI if the resolution's dynamic scale exceeds this limit.\n\n" ..
		"Open and close a window to enact changes."
	}
}